prefix		= @prefix@
exec_prefix	= @exec_prefix@
bindir 		= @bindir@
datadir		= @datadir@/@PACKAGE@
INSTALL		= @INSTALL@
data		= ${datadir}/data
pycomp		= ${srcdir}/py-compile

@SET_MAKE@

all:

install:
	${INSTALL} -d ${data}
	${INSTALL} gedcom.xml ${data}

uninstall:

clean:


